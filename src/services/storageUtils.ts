
// Utility function to sync all databases to storage
export const syncDatabaseToStorage = (): void => {
  // This function is called from other services when they update their data
  // It serves as a central place to handle any cross-database synchronization
  // Currently it's a placeholder as each service handles its own storage
  // but it's useful for future expansion
};
